# API standards (imported into CLAUDE.md via @import — Domain 3, Task 3.1)
- Every endpoint validates input and returns structured errors.
- Money is always integer cents.
- Version breaking changes; never silently alter response shapes.
