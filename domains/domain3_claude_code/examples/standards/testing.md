# Testing standards (imported into CLAUDE.md via @import — Domain 3, Task 3.1)
# @import keeps the root CLAUDE.md small; each package imports only what it needs.
- Aim for behavior coverage, not line coverage.
- Prefer fast unit tests; reserve integration tests for boundaries.
- Test the empty-input and error cases before the happy path.
