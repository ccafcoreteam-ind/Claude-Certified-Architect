{
  "_comment": "Example PROJECT-scoped MCP config (Domain 2, Task 2.4). Because this file lives in the repo, every teammate gets these servers via version control. Secrets use ${ENV_VAR} expansion so the file is safe to commit. Personal/experimental servers belong in ~/.claude.json instead.",
  "mcpServers": {
    "jira": {
      "command": "npx",
      "args": ["-y", "@community/mcp-jira"],
      "env": {
        "JIRA_BASE_URL": "${JIRA_BASE_URL}",
        "JIRA_TOKEN": "${JIRA_TOKEN}"
      }
    },
    "team-postgres": {
      "command": "npx",
      "args": ["-y", "@community/mcp-postgres"],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  }
}
